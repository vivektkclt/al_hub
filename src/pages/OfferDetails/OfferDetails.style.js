import { C } from "../../assets";
import { StyleSheet } from "react-native";

export const offerDetailStyle = StyleSheet.create({
  mainStyle: {
    padding: 20,
    backgroundColor: C.colors.primary.primary,
  },
});
