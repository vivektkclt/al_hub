import { C } from "../../assets";
import { StyleSheet } from "react-native";

export const StoreDetailStyle = StyleSheet.create({
  tab: {
    paddingTop: 10,
    paddingBottom: 10,
    borderBottomWidth: 0.2,
    borderColor: C.colors.border.grey,
    backgroundColor: C.colors.primary.color1,
  },
});
